# Author links are intentionally disabled: all co-author names render as
# plain text. The author's own name (Eunhan Ka) is still emphasized via the
# scholar self-name styling in the bibliography template, not from here.
